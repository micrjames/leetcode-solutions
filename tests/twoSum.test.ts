const { twoSum } = require("../twoSum");

describe("An array.", () => {
   test.todo("Should add up to the target by brute force.");
   test.todo("Should add up to the target by difference from Map.");
});
