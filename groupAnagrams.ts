// 49. Group Anagrams
// Given an array of strings strs, group the anagrams together. You can return the answer in any order.
// a. Sort & Compare

const SortCmp = (strs: string[]): string[][] => {
};

// b. Map 
const MapCmp = (strs: string[]): string[][] => {
};

exports.groupAnagrams = { SortCmp, MapCmp };
